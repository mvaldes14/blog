server {
    listen 80;
    server_name blog.mvaldes.dev;
    root /usr/share/nginx/html;
    index index.html;
    error_page 404 /404.html;
 
    location = / {
        return 301 /en;
    }

    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
