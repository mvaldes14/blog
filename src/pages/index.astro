---
// Root index — redirect to default language.
// In the future this could detect Accept-Language and route smartly,
// but a static redirect is simpler and works fine for now.
return Astro.redirect('/en');
---
