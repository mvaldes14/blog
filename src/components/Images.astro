---
type Props = {
  src: string;
  alt: string;
};
const { src, alt } = Astro.props;
const baseURL = "https://s3.mvaldes.dev/blog";
const url = `${baseURL}/${src}`;
---
<img class="mx-auto" src={url} alt={alt} />
