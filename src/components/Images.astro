---
type Props = {
  src: string;
  alt: string;
};
const { src, alt } = Astro.props;
const baseURL = "https://pub-5937e710ea9840539571d58d00f863c1.r2.dev";
const url = `${baseURL}/${src}`;
---
<img class="mx-auto" src={url} alt={alt} />
